#include <stdio.h>

int main()
{
    
    float v1, v2;
    float media;
    
    printf ("Primeiro valor: ");
    scanf ("%d", &v1);
    
    printf("Segundo valor: ");
    scanf("%d", &v2);
    
    media = (v1 + v2)/2 ;
    printf("A média de %f e %f = %f", v1,v2,media);

    
    return 0;
}